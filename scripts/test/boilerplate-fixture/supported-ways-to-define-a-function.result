function foo() { eval "$(beginFunction)"; 
    :
}

function foo()
{ eval "$(beginFunction)"; 
    :
}

foo() { eval "$(beginFunction)"; 
    :
}

foo()
{ eval "$(beginFunction)"; 
    :
}

foo() { eval "$(beginFunction)";  :; }
